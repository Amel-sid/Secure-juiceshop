# ===================================================================
# DÉPLOIEMENT SÉCURISÉ DE JUICE SHOP
# ===================================================================
#
# Ce rôle déploie l'application OWASP Juice Shop dans un container Docker
# avec des paramètres de sécurité renforcés pour la démonstration.
#
# Juice Shop est une application web volontairement vulnérable utilisée
# pour l'apprentissage de la sécurité. On la sécurise au niveau infrastructure
# tout en gardant ses vulnérabilités internes pour les tests.
#
# CONFORMITÉ :
# - ISO 27001 A.14.1.3 : Protection des transactions de services applicatifs
# - ISO 27001 A.13.1.1 : Contrôles de réseau (isolation)
# - HDS Article 9 : Isolation des environnements

---
# Nettoyer les installations précédentes pour éviter les conflits
# Si un ancien container Juice Shop existe, on le supprime proprement
# Le force_kill assure qu'on peut supprimer même un container qui plante
- name: Arrêter conteneur Juice Shop existant
  docker_container:
    name: juice-shop
    state: absent
    force_kill: yes
  become: yes
  ignore_errors: yes  # On ignore les erreurs si aucun container n'existe
  tags: juice-shop

# Créer un réseau Docker dédié pour isoler Juice Shop
# L'isolation réseau empêche Juice Shop de communiquer avec d'autres containers
# C'est un principe de sécurité : chaque application dans son propre réseau
- name: Créer réseau Docker isolé
  docker_network:
    name: "{{ juice_shop_network }}"
    driver: bridge          # Type de réseau standard pour un container unique
    state: present
  become: yes
  tags: juice-shop

# Télécharger l'image officielle Juice Shop depuis Docker Hub
# On utilise des retry car le téléchargement peut échouer (problème réseau)
# Timeout de 5 minutes car l'image fait environ 150MB
- name: Télécharger image Juice Shop
  docker_image:
    name: "{{ juice_shop_image }}"
    tag: "{{ juice_shop_version }}"
    source: pull
    timeout: 300            # 5 minutes max pour télécharger
  become: yes
  retries: 3                # 3 tentatives si échec
  delay: 30                 # Attendre 30s entre chaque tentative
  register: pull_result
  until: pull_result is succeeded
  tags: juice-shop

# Démarrer le container Juice Shop avec une configuration sécurisée
# Cette config limite les ressources et applique des restrictions de sécurité
- name: Lancer conteneur Juice Shop sécurisé
  docker_container:
    name: juice-shop
    image: "{{ juice_shop_image }}:{{ juice_shop_version }}"
    state: started
    restart_policy: unless-stopped  # Redémarre automatiquement si crash
    
    # Configuration réseau : on expose seulement le port nécessaire
    # published_ports:
    #   - "{{ juice_shop_external_port }}:{{ juice_shop_internal_port }}"
    networks:
      - name: "{{ juice_shop_network }}"
    
    # Variables d'environnement pour configurer l'application
    env:
      NODE_ENV: production              # Mode production plus sécurisé
      PORT: "{{ juice_shop_internal_port | string }}"
      APPLICATION_CHATBOT: "false"     # Désactive le chatbot qui peut poser des problèmes
      CHATBOT_TRAINING_DATA: ""        # Pas de données de training
      SOLUTIONS_WEBHOOK: ""            # Pas de webhook externe
    
    # Limitation des ressources pour éviter que l'app consomme tout le serveur
    memory: 512m                # Maximum 512MB de RAM
    memory_swap: 512m           # Pas de swap supplémentaire
    cpu_quota: 50000            # Maximum 50% d'un CPU (sur 100000)
    
    # Paramètres de sécurité du container
    security_opts:
      - "no-new-privileges:true"        # Empêche l'escalade de privilèges
    
    # Système de fichiers temporaire sécurisé
    tmpfs:
      - /tmp:noexec,nosuid,size=100m    # /tmp en mémoire, pas d'exécution de fichiers
    
  become: yes
  register: juice_container
  tags: juice-shop

# Attendre que l'application soit complètement démarrée
# Juice Shop prend quelques secondes à initialiser après le démarrage du container
# - name: Attendre démarrage complet de Juice Shop
#   wait_for:
#     host: localhost
#     port: "{{ juice_shop_external_port }}"
#     timeout: 60             # Attendre maximum 1 minute
#   tags: juice-shop

# Vérifier que l'application répond correctement
# Test basique pour s'assurer que Juice Shop est accessible
# ignore_errors pour éviter que le déploiement plante si l'app n'est pas encore prête
- name: Vérifier que Juice Shop répond
  uri:
    url: "http://localhost:{{ juice_shop_external_port }}"
    method: GET
    timeout: 10
  register: juice_check
  retries: 3
  delay: 5
  ignore_errors: yes       # Ne pas faire planter Ansible si l'app n'est pas prête
  tags: juice-shop

# Rapport final du déploiement avec status conditionnel
# Affiche les informations importantes même si le test de santé a échoué
- name: Rapport déploiement Juice Shop
  debug:
    msg: |
      ================================
      JUICE SHOP DÉPLOYÉ ET SÉCURISÉ
      ================================
      Status: {{ 'ACTIF' if (juice_check is defined and juice_check.status is defined and juice_check.status == 200) else 'EN COURS DE DÉMARRAGE' }}
      URL locale: http://localhost:{{ juice_shop_external_port }}
      Container ID: {{ juice_container.container.Id[:12] }}
      
      Sécurisations appliquées:
      - Container isolé dans réseau dédié
      - Ressources limitées (512MB RAM, 50% CPU)
      - Pas d'escalade de privilèges
      - Système /tmp en mémoire (noexec)
      - Redémarrage automatique si crash
      
      {% if juice_check is defined and juice_check.status is defined and juice_check.status == 200 %}
      ✅ Application accessible et fonctionnelle
      {% else %}
      ⏳ Application en cours de démarrage (normal)
      💡 Réessayez dans quelques secondes si besoin
      {% endif %}
      ================================
  tags: juice-shop